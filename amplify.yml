version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd Frontend
        - pnpm install
    build:
      commands:
        - cd Frontend
        - pnpm run build
  artifacts:
    baseDirectory: Frontend/.next
    files:
      - '**/*'
  cache:
    paths:
      - Frontend/node_modules/**/*
